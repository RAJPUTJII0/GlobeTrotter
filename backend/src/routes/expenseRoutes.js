import { Router } from 'express'; import { createExpense, deleteExpense, getExpenses } from '../controllers/expenseController.js'; import { getBudget } from '../controllers/budgetController.js'; import { requireAuth } from '../middleware/authMiddleware.js'; const router=Router(); router.use(requireAuth); router.get('/:tripId/expenses',getExpenses); router.post('/:tripId/expenses',createExpense); router.delete('/:tripId/expenses/:expenseId',deleteExpense); router.get('/:tripId/budget',getBudget); export default router;
