import { useEffect, useState } from 'react'; import { useParams } from 'react-router-dom'; import { getPublicTrip } from '../services/tripService.js';
export default function PublicTrip(){const {slug}=useParams();const [trip,setTrip]=useState(null);const [error,setError]=useState('');useEffect(()=>{getPublicTrip(slug).then(r=>setTrip(r.trip)).catch(e=>setError(e.message));},[slug]);if(error)return <main><p role="alert">{error}</p></main>;if(!trip)return <main><p>Loading shared trip...</p></main>;return <main style={{maxWidth:760,margin:'0 auto',padding:32}}><h1>{trip.title}</h1><p>{trip.description}</p><p>{trip.startDate} to {trip.endDate}</p><h2>Stops</h2><ul>{trip.stops.map(s=><li key={s.id}>{s.city}: {s.startDate} to {s.endDate}</li>)}</ul></main>}
